To run this dashboard for yourself, you will need an API key from TMDb (The Movie Database). 

Enter the API key into a file called 'secrets.toml'. You can find an example in the secrets.toml.template file.